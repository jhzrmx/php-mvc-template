# Required: at least 32 characters for JWT signing
JWT_SECRET=your-super-secret-key-at-least-32-chars-long

# Optional: token lifetime in seconds (default 3600 = 1 hour)
JWT_EXPIRES_IN=3600

# Database (RedBeanPHP)
# DB_DSN=mysql:host=127.0.0.1;dbname=myapp
# DB_USER=root
# DB_PASS=secret
